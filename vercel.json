{
  "version": 2,
  "builds": [{ "src": "/app.js", "use": "@vercel/node" }],
  "routes": [
    { "src": "/users", "dest": "/app.js" },
    { "src": "/src/routes/(.*)", "dest": "/app.js" }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
