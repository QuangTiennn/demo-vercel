{
  "version": 2,
  "builds": [{ "src": "/app.js", "use": "@vercel/node" }],
  "routes": [
    { "src": "/users", "dest": "/app.js" },
    { "src": "/(.*)", "dest": "/app.js" }
  ],
  "rewrites": [{ "source": "/api/(.*)", "destination": "/api" }],
  "env": {
    "NODE_ENV": "production"
  }
}
